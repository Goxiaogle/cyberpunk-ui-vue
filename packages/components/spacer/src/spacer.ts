import type { ExtractPropTypes } from 'vue'

/**
 * @category 基础组件
 * @displayName CpSpacer 间距
  * @description 间距组件，用于快速在元素之间添加间距
 */
export const spacerProps = {} as const

export type SpacerProps = ExtractPropTypes<typeof spacerProps>
